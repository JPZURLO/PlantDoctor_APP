<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.DiagnoseFragment"
    android:padding="16dp">

    <!-- ✅ SUBSTITUÍDO O TEXTVIEW POR UM BOTÃO ✅ -->
    <Button
        android:id="@+id/btn_start_diagnosis"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Iniciar Diagnóstico"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.4"
        android:padding="16dp"/>

    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="Clique no botão acima para pedir permissão da câmara e selecionar a sua cultura."
        app:layout_constraintEnd_toEndOf="@id/btn_start_diagnosis"
        app:layout_constraintStart_toStartOf="@id/btn_start_diagnosis"
        app:layout_constraintTop_toBottomOf="@id/btn_start_diagnosis" />

</androidx.constraintlayout.widget.ConstraintLayout>
