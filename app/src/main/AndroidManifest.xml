<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/plantsos"
        android:roundIcon="@mipmap/plantsos_round"
        android:supportsRtl="true"

        android:label="@string/app_name"

        android:theme="@style/Theme.PlantdSoS"

        android:usesCleartextTraffic="true"
        tools:targetApi="26"> <activity
        android:name=".activities.MainActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

        <activity
            android:name=".activities.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".activities.CultureSelectionActivity"
            android:exported="false" />

        <activity
            android:name=".activities.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".activities.OnboardingActivity"
            android:exported="false" />

        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ManageCulturesActivity"
            android:exported="false" />

        <activity
            android:name=".activities.RecoveryActivity"
            android:exported="false" />

        <activity
            android:name=".activities.CreateNewPasswordActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="reset-password"
                    android:scheme="plantdoctor" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.CultureDetailsActivity"
            android:exported="false"
            android:label="Detalhes da Cultura"
            android:parentActivityName=".activities.HomeActivity"

            />

        <activity
            android:name=".activities.PlantingCalendarActivity"
            android:exported="false" />

        <activity
            android:name=".activities.EditUserActivity"
            android:exported="false" />

        <!-- ✅ BLOCO DO FILEPROVIDER CORRIGIDO ✅ -->
        <!-- Garante que tem os prefixos 'android:' corretos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
        <!-- ✅ FIM DO BLOCO ✅ -->

    </application>

</manifest>

